import SwiftUI

public enum DesignSystem {
    public static let primaryGradient = Gradient(colors: [Color.purple, Color.blue])
    public static let cornerRadius: CGFloat = 16
    public static let defaultPadding: CGFloat = 20
}
